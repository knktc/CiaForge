{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default CiaForge window capability.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-size",
    "dialog:default",
    "opener:default"
  ]
}
